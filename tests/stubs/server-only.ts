// Stub for the `server-only` package in the Vitest environment.
// The real package throws when imported outside a React Server Component;
// tests run in Node directly, so this is a no-op replacement (see
// vitest.config.mts alias).
export {};
